# ${XDG_CONFIG_HOME}/zsh/conf.d/50-core.zsh

# ZSH-Specific Aliases

# Many terminals use Ctrl-s and Ctrl-q for flow control by default. This
# interferes with using Ctrl-r and Ctrl-s for history searching. Disable it.
stty stop undef